import {author, category, siteConfig, page, post, route} from './documents'
import {
	blockContent,
	ctaButton,
	simplePortableText,
	link,
	internalLink,
	heroCarousel,
	colorText,
} from './objects'

export const schemaTypes = [
	post,
	author,
	category,
	ctaButton,
	blockContent,
	link,
	internalLink,
	page,
	route,
	simplePortableText,
	siteConfig,
	heroCarousel,
	colorText,
]
