# Search Feature — Test Case Inventory

## Purpose

A list of every area of the public search surface, and for each area, the test cases that
should exist with their expected result - written **before** any test code, from what the
feature should do, not from reading the implementation and reverse-describing what it
currently does. See [Test Automation Plan](README.md#test-automation-plan) in the main
testing doc for why: a test derived from the code can only confirm the code's current
behavior, bugs included; a test derived from expected behavior fails against a bug instead
of enshrining it.

This is a coverage-tracking document, not a spec of intent frozen in time. As cases are
turned into real tests, update their **Status**. As real product decisions change expected
behavior, update the **Expected** column - don't let this drift into just describing
whatever the code happens to do today.

**When a new test correctly fails against real app behavior** (not a bad mock/assertion - an
actual bug), file it as a GitHub issue in this repo (matching how the rest of the team already
tracks work - see `#2055`-`#2063`), reference the issue number in this doc's Status cell for
that case, and reference the test file back in the issue. The failing test stays failing/red -
don't weaken the assertion to make it pass, and don't mark the case "Covered" without the
"failing, confirms a real bug" note. #2058/#2059/#2060/#2061 are the reference example for this.

**Same treatment for a "worth noting" finding that isn't (yet) a demonstrated bug** - a
fragile-but-currently-working assumption (#2062), an anomaly whose root cause needs more
digging (#2063). Don't just leave these as a comment in this doc where they're easy to lose -
file them too, using the right one of this org's issue templates/types (`gh issue create` from
`.github/ISSUE_TEMPLATE/` in the org-level `weareinreach/.github` repo - "Bug Report" for
user-observable-but-not-fully-root-caused problems, "Codebase Improvement" for
not-broken-yet-but-fragile code, set via `gh api -X PATCH .../issues/<n> -f type="Bug"` or
`-f type="Task"` to match the repo's actual issue-type feature, not just labels).

**Every issue filed this way, bug or note, needs a "Real-world impact" section** - not just
the technical root cause. Explain what an actual user experiences and why it matters for this
specific app (people looking for LGBTQ+ resources, sometimes urgently) - "this returns the
wrong HTTP status" is not the same information as "this looks like the site is down to someone
who may not try again." Add it as a follow-up comment if the root-cause writeup came first;
see #2058-#2061's comments for the pattern.

**Columns:**

- **Layer** - which test tool this case belongs in, per the three-layer split in the main
  doc: **Vitest** (component logic, mocked network), **Chromatic** (visual/CSS layout via
  Storybook snapshot), **Playwright** (real browser, full flow), or **Manual** (not
  practical to automate yet - documented so it isn't silently skipped).
- **Status** - `Not started` / `Partial` / `Covered`, with a pointer to the test file once
  one exists.

This first pass was built from reading the actual current implementation to find where
behavior branches (states, edge cases, conditionals) - that's a legitimate way to find
_where_ to write test cases. The **Expected** column, however, states what should happen,
independent of whether the code agrees; anywhere it doesn't agree with what you observe in
the running app, that's a bug to file, not a reason to change the Expected column to match.

---

## 1. URL / Route Parsing

Route: `/search/[country]/[lon]/[lat]/[dist]/[unit]`, plus `?page=`.

| #   | Given                                               | When        | Expected                                                                                              | Layer                | Status                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --- | --------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------- | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1.1 | A valid 5-segment URL (`us/-122.1/37.4/50/mi`)      | Page loads  | Results render for that location/radius/unit                                                          | Playwright           | Covered (`home.spec.ts`, via the search-box flow)                                                                                                                                                                                                                                                                                                                                                                                     |
| 1.2 | A malformed URL (missing/non-numeric segment)       | Page loads  | A styled, user-facing error state renders - not a raw crash                                           | Playwright           | Covered (`url-routing.spec.ts`) - **failing, confirms a real bug, filed as [#2058](https://github.com/weareinreach/InReach/issues/2058)**: `getServerSideProps` calls `SearchParamsSchema.parse(query.params)` (throwing, not `.safeParse()`), so this actually 500s with an uncaught `ZodError` - worse than the originally-suspected "bare Error text," since it never reaches the client component's own safeParse fallback at all |
| 1.3 | `?page=3` with a valid params tuple                 | Page loads  | Page 3's results render, matching the URL, not page 1                                                 | Playwright           | Covered (`url-routing.spec.ts`) - passing                                                                                                                                                                                                                                                                                                                                                                                             |
| 1.4 | `?page=` set to a non-numeric or out-of-range value | Page loads  | Falls back to page 1 rather than crashing or showing an empty/broken state                            | Vitest or Playwright | Covered (`url-routing.spec.ts`) - **failing, confirms a real bug, filed as [#2059](https://github.com/weareinreach/InReach/issues/2059)**: `PageIndexSchema`'s `.default(1)` only covers an _absent_ `page` param; a present-but-non-numeric value coerces to `NaN`, which `.parse()` rejects, producing the same uncaught-crash class of bug as 1.2                                                                                  |
| 1.5 | User clicks a pagination link                       | URL updates | `?page=` reflects the new page via shallow update; no full page reload/re-run of `getServerSideProps` | Playwright           | Covered (`url-routing.spec.ts`) - passing                                                                                                                                                                                                                                                                                                                                                                                             |

## 2. Location Search (`SearchBox`, location mode)

| #   | Given                                                                | When                                        | Expected                                                                                                                                                                                                                        | Layer                                 | Status                                                                                                                                                                                                                                                                               |
| --- | -------------------------------------------------------------------- | ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 2.1 | User types a real place name                                         | 400ms debounce elapses                      | Autocomplete suggestions appear                                                                                                                                                                                                 | Playwright                            | Covered (`home.spec.ts`)                                                                                                                                                                                                                                                             |
| 2.2 | User types a place name                                              | Suggestions are still loading               | A loading indicator shows; no stale/empty dropdown flashes                                                                                                                                                                      | Vitest                                | Covered (`SearchBox.test.tsx`)                                                                                                                                                                                                                                                       |
| 2.3 | User types a query with no matching places                           | Debounce elapses                            | No results state shown, no dropdown box with empty content                                                                                                                                                                      | Vitest                                | Covered (`SearchBox.test.tsx`) - note: this only triggers when the upstream API itself reports `status: 'ZERO_RESULTS'`, not merely an empty `results` array; worth knowing if a future mock/fixture omits `status` and the case silently stops passing for the wrong reason         |
| 2.4 | User selects a suggestion                                            | Selection resolves                          | Browser navigates to `/search/[...params]` with that location's coordinates, default radius (200) and unit (mi)                                                                                                                 | Playwright                            | Covered (`home.spec.ts`)                                                                                                                                                                                                                                                             |
| 2.5 | User had a custom radius set (e.g. 50mi) on the current results page | User searches a new location and selects it | _Decide the expected behavior_ - does it reset to the 200mi default (current behavior) or preserve the user's chosen radius? This is a real product decision, not an implementation detail - resolve it before writing the test | Playwright                            | Not started - needs a product decision first                                                                                                                                                                                                                                         |
| 2.6 | User presses Enter with suggestions showing                          | -                                           | The top suggestion is submitted, without requiring it to be visually highlighted first                                                                                                                                          | Vitest                                | Covered (`SearchBox.test.tsx`)                                                                                                                                                                                                                                                       |
| 2.7 | User has typed text in the box                                       | User clicks the clear (✕) affordance        | Input clears, any dropdown closes                                                                                                                                                                                               | Vitest                                | Covered (`SearchBox.test.tsx`)                                                                                                                                                                                                                                                       |
| 2.8 | The geocoding API call fails (network error, rate limit, etc.)       | User selects a suggestion                   | A user-facing error/retry state shows - not a silent failure to navigate                                                                                                                                                        | Playwright or Vitest (mocked failure) | Covered (`SearchBox.test.tsx`) - **failing, confirms a real bug, filed as [#2060](https://github.com/weareinreach/InReach/issues/2060)**: `geoByPlaceId`'s `error`/`isError` are never read by the component at all, so this can't show an error regardless of what the network does |

**Story-coverage gap found auditing this section**: none of `SearchBox.stories.tsx`'s 7 stories
(`ByLocation`, `ByOrganization`, all 5 `SmartSearch*`) contain a `play` function, so the
`Combobox.Dropdown` never actually opens in any of them - the matched-text highlight, location
result rows, org "no results"/"suggest a resource" rows, and per-option loading spinner are
**all unrendered in Storybook today**, regardless of what mock data exists. This is the same
class of gap as the ServiceFilter/MoreFilter badge finding (§7/§8) - Chromatic has nothing to
snapshot for the dropdown-open state, so a library update breaking it would pass every existing
check. Fixing this needs a `play` function (Storybook's interaction-testing API) that focuses
the input and types a query, not just new `args` - add one story per mode with a `play` step
before writing the Chromatic case below.

| 2.9 | `SearchBox` (location mode) story | A `play` function focuses the input and types a query | Dropdown opens and renders real result rows - the actual visual target for a library-update regression check | Chromatic | Not started - blocked on the `play`-function story fix above |

## 3. Organization Name Search (`SearchBox`, organization mode)

| #   | Given                                                   | When                             | Expected                                                                   | Layer  | Status                                                                                                                                                                                                                                                                      |
| --- | ------------------------------------------------------- | -------------------------------- | -------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 3.1 | User types a real org name                              | Debounce elapses                 | Matching orgs appear, with the matched substring visually highlighted      | Vitest | Covered (`SearchBox.test.tsx`)                                                                                                                                                                                                                                              |
| 3.2 | User types a query with no matching orgs                | Debounce elapses, non-blank term | A "suggest a resource" option appears, routing to `/suggest` when selected | Vitest | Covered (`SearchBox.test.tsx`)                                                                                                                                                                                                                                              |
| 3.3 | Box receives focus with no text typed                   | -                                | Dropdown does **not** open on bare focus                                   | Vitest | Covered (`SearchBox.test.tsx`)                                                                                                                                                                                                                                              |
| 3.4 | User selects an org result                              | Selection resolves               | Browser navigates to `/org/[slug]` for that org                            | Vitest | Covered (`SearchBox.test.tsx`) - done as Vitest instead of the originally-planned Playwright layer; verifying `router.push` is called correctly doesn't need a real browser, and sidesteps a real jsdom limitation with Mantine's `Combobox` (see README's jsdom gaps note) |
| 3.5 | A previous location search set `searchState.searchTerm` | Org search box mounts            | Org box does **not** pre-fill with the leftover location text              | Vitest | Covered (`SearchBox.test.tsx`)                                                                                                                                                                                                                                              |

Same dropdown-never-opens gap as §2.9 applies here too (org mode's own 5 `SmartSearch*`
stories, same missing `play` function) - not duplicated as a separate case, same fix covers
both modes.

## 4. Search Results Rendering

| #   | Given                                                                                                | When           | Expected                                                                                                                                                | Layer                   | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | ---------------------------------------------------------------------------------------------------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 4.1 | Results query hasn't resolved yet                                                                    | Page renders   | Skeleton placeholder cards show, not blank space or an error                                                                                            | Playwright              | Covered (`results-rendering.spec.ts`) - done as Playwright (route delay) instead of Vitest, since this is page-level rendering, not an isolated component                                                                                                                                                                                                                                                                                                                       |
| 4.2 | Results query returns 0 orgs                                                                         | Query resolves | Empty-results messaging + crisis-support resources render instead of a blank list; sidebar still renders                                                | Playwright              | Covered (`results-rendering.spec.ts`)                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 4.3 | Results query returns orgs across multiple proximity tiers                                           | Query resolves | A tier-header divider appears the first time each new tier (Neighborhood/Local/Region/etc.) appears in the ordered list, not once per item in that tier | Playwright              | Covered (`results-rendering.spec.ts`) - proves it for Neighborhood/Local (both present on page 1 at the test location); Regional/Extended/National tiers aren't reachable on page 1 there but exercise the identical code path                                                                                                                                                                                                                                                  |
| 4.4 | Results query fails (network/server error)                                                           | Query rejects  | A user-facing error state renders - confirm this actually happens; the survey found no obviously-handled error path here                                | Vitest (mocked failure) | **Confirmed a real bug via code reading, filed as [#2061](https://github.com/weareinreach/InReach/issues/2061)** - not yet an automated test (reliably forcing this query to fail against a real backend isn't straightforward from Playwright, and the page component isn't independently importable the way `SearchBox` is for a Vitest test)                                                                                                                                 |
| 4.5 | User is on the last page and changes a filter that reduces total pages below the current page number | Filter applied | User isn't left on a now-out-of-range empty page silently                                                                                               | Playwright              | Not started - filed as [#2063](https://github.com/weareinreach/InReach/issues/2063): `?page=99` on a real 352-result location unexpectedly returns `resultCount: 0`, not the true total - `orgs.total` in `query.searchDistanceV3.handler.ts` appears to depend on the skip/take window rather than being a true grand total; root cause not yet fully traced (filed honestly as such), needed before this case itself (about pagination UI state, not the count) can be tested |

## 5. Result Card Content & Navigation

| #   | Given                                                               | When         | Expected                                                                                                             | Layer              | Status                                                                                                                                                                                                                    |
| --- | ------------------------------------------------------------------- | ------------ | -------------------------------------------------------------------------------------------------------------------- | ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 5.1 | An org serves 4+ distinct cities                                    | Card renders | City list shows up to 3 plus "and N more", deduplicated case-insensitively                                           | Vitest             | Covered (`SearchResultCard.test.tsx`)                                                                                                                                                                                     |
| 5.2 | An org is marked `national`                                         | Card renders | No city list shown at all                                                                                            | Vitest             | Covered (`SearchResultCard.test.tsx`)                                                                                                                                                                                     |
| 5.3 | An org's `addressVisibility` is `HIDDEN`                            | Card renders | No city list shown, regardless of `national`                                                                         | Vitest             | Covered (`SearchResultCard.test.tsx`)                                                                                                                                                                                     |
| 5.4 | An org serves multiple countries                                    | Card renders | A "national"/multi-country badge shows in addition to any leader badge                                               | Vitest             | Confirmed already covered by the existing `MultipleResults` Storybook story - see the note below this table                                                                                                               |
| 5.5 | Org-specific translation override exists for `description`          | Card renders | The per-org override text is used, not the generic fallback                                                          | Vitest             | Covered (`SearchResultCard.test.tsx`)                                                                                                                                                                                     |
| 5.6 | User clicks the org name/title                                      | Click fires  | Navigates to `/org/[slug]`; an analytics profile-view event fires with correct position/index/distance/tier metadata | Vitest             | Covered (`SearchResultCard.test.tsx`) - verifies the link's slug and asserts `productEvent.profileView` is called with the correct position/searchVersion/distanceMeters                                                  |
| 5.7 | An org has a non-empty `national` array (serves multiple countries) | Card renders | `Badge.National` shows                                                                                               | Vitest + Chromatic | Not started - no mock org in `organization.searchDistance.json`/`organization.searchDistanceLongTitle.json` has `national` populated, so this doesn't render in any story either; needs a new mock entry, not just a test |
| 5.8 | An org has `addressVisibility === 'HIDDEN'`                         | Card renders | City list suppressed (already an Expected in 5.3, repeated here specifically as a story/Chromatic gap)               | Chromatic          | Not started - no mock org has `addressVisibility` set at all                                                                                                                                                              |
| 5.9 | User hovers the card title                                          | -            | `data-hovered` styling (underline via `.hoverText[data-hovered]`) applies                                            | Chromatic          | Not started - `SearchResultCard.stories.tsx` has no `play` function anywhere, so `:hover` is never simulated in any snapshot                                                                                              |

(5.4's leader/community-focus badges and the "and N more" city overflow **are** already
exercised by the existing `MultipleResults` story - confirmed, not a gap - listed here only so
it's clear which parts of this section were checked and found fine, not skipped.)

## 6. Favoriting / Saved Lists (`ActionButtons.Save`)

| #   | Given                                             | When                         | Expected                                                                                                                              | Layer                      | Status                                                                                                                                                                                                                                               |
| --- | ------------------------------------------------- | ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 6.1 | User is not logged in                             | User clicks Save on a result | An auth-promotion modal opens; nothing is saved                                                                                       | Vitest (mock `useSession`) | Covered (`Save.test.tsx`)                                                                                                                                                                                                                            |
| 6.2 | User is logged in, item not yet saved to any list | User clicks Save             | A menu of the user's lists opens, including "create new list"                                                                         | Vitest                     | Covered (`Save.test.tsx`) - filed the `Boolean(savedToLists)` fragility this case's mock exposed as [#2062](https://github.com/weareinreach/InReach/issues/2062) (see the note below this table)                                                     |
| 6.3 | User is logged in, item saved to exactly one list | User clicks Save             | Item is removed from that list immediately (no menu)                                                                                  | Vitest                     | Covered (`Save.test.tsx`)                                                                                                                                                                                                                            |
| 6.4 | User is logged in, item saved to multiple lists   | User clicks Save             | Menu opens showing membership per list, not a single direct unsave                                                                    | Vitest                     | Covered (`Save.test.tsx`)                                                                                                                                                                                                                            |
| 6.5 | Save mutation succeeds                            | -                            | Success toast shows; item's save state updates immediately without a page refresh (real-cache contract - see main testing doc)        | Vitest, real-cache harness | Not started - blocked on extending `~ui/test/trpcIntegrationHarness` (or a new sibling harness) with a fake `savedList` backend; the existing harness only covers org-phone CRUD. This is real, separate infrastructure work, not a quick add        |
| 6.6 | Save mutation fails (network error)               | -                            | Error toast shows; item's displayed save state does not silently flip to "saved"                                                      | Vitest (mocked failure)    | Covered (`Save.test.tsx`)                                                                                                                                                                                                                            |
| 6.7 | The "get all lists" fetch fails                   | Menu opens                   | A retry option shows in the menu, not a permanently broken/empty menu                                                                 | Vitest                     | Covered (`Save.test.tsx`)                                                                                                                                                                                                                            |
| 6.8 | Logged in, item saved to one or multiple lists    | Menu is opened (clicked)     | The actual open `Menu.Dropdown` visual (dark background, white item text, hover-underline) renders - not just the closed heart button | Chromatic                  | Not started - `ActionButtons/index.stories.tsx` has stories reaching this code path (`Save`, `SavedToMultipleLists`) but none has a `play` function to actually click and open the menu, so the dropdown's own appearance has never been snapshotted |

Filled-vs-outline heart / "Saved"-vs-"Save" label (the other half of this component's visual
state) **is** already covered across the `SaveLoggedOut`/`Save`/`SavedToSingleList`/
`SavedToMultipleLists` stories - confirmed fine, not a gap.

**A real fragility found while writing 6.2, not a live bug**: `Save.tsx` computes
`const isSaved = Boolean(savedToLists)`. `Boolean([])` is `true` in JS - an empty array is
truthy - so this only correctly means "not saved" today because the real API
(`packages/api/router/savedLists/query.isSaved.handler.ts`) specifically returns `null`
(never `[]`) when an item isn't saved anywhere. The app isn't buggy right now, but the
correctness of "not saved" silently depends on that API detail rather than being defensively
coded (`savedToLists && savedToLists.length > 0` would be correct regardless of which falsy-ish
value the API chooses). Nothing is broken today, but filed as a Codebase Improvement -
[#2062](https://github.com/weareinreach/InReach/issues/2062) - since silently losing the
ability to tell what a user has actually saved (if this ever tips over) is exactly the kind of
thing worth catching before it happens, not after.

## 7. Service Filter (`ServiceFilter`)

| #    | Given                                                    | When                           | Expected                                                                                                                                                                    | Layer  | Status                                                                                                                                                    |
| ---- | -------------------------------------------------------- | ------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 7.1  | Filter options are loading                               | Modal opens                    | Skeleton placeholders show                                                                                                                                                  | Vitest | Covered (`modals/ServiceFilter/index.test.tsx`)                                                                                                           |
| 7.2  | User checks an individual sub-service                    | -                              | Only that sub-service is added to `searchState.services`; results refetch reactively                                                                                        | Vitest | Covered (`modals/ServiceFilter/index.test.tsx`) - **failing, confirms a real bug, filed as [#2064](https://github.com/weareinreach/InReach/issues/2064)** |
| 7.3  | User checks a category's "select all" checkbox           | -                              | All sub-services in that category are selected; checkbox itself shows checked (not indeterminate)                                                                           | Vitest | Covered (`modals/ServiceFilter/index.test.tsx`) - **failing, confirms a real bug, filed as [#2064](https://github.com/weareinreach/InReach/issues/2064)** |
| 7.4  | Some but not all sub-services in a category are selected | Category checkbox renders      | Shows indeterminate state                                                                                                                                                   | Vitest | Covered (`modals/ServiceFilter/index.test.tsx`) - **failing, confirms a real bug, filed as [#2064](https://github.com/weareinreach/InReach/issues/2064)** |
| 7.5  | User selects several services then closes the modal      | Modal closes                   | Selections persist (results already updated reactively before close - closing is presentational only, doesn't itself trigger the query)                                     | Vitest | Covered (`modals/ServiceFilter/index.test.tsx`)                                                                                                           |
| 7.6  | Component receives `disabled` (intl fallback pages)      | Renders                        | Filter is visibly present but non-interactive                                                                                                                               | Vitest | Covered (`modals/ServiceFilter/index.test.tsx`)                                                                                                           |
| 7.7  | No services selected                                     | Renders                        | No count badge shows anywhere (neither the overall trigger badge nor any per-category one) - `selectedItemCount > 0` / per-category `selectedCount > 0` guards both         | Vitest | Covered (`modals/ServiceFilter/index.test.tsx`)                                                                                                           |
| 7.8  | User selects N services across one or more categories    | Badge renders                  | Overall badge (on the filter trigger) shows the exact total count; each affected category's own badge shows that category's own count, not the total                        | Vitest | Covered (`modals/ServiceFilter/index.test.tsx`)                                                                                                           |
| 7.9  | User deselects back down to zero                         | Badge re-renders               | Both badges disappear again (not just show "0")                                                                                                                             | Vitest | Covered (`modals/ServiceFilter/index.test.tsx`)                                                                                                           |
| 7.10 | Results query is loading vs. resolved                    | Modal's confirm button renders | Button text reflects `resultCount` via `t('view-x-result', { count: resultCount })`, correctly pluralized, and updates once the count changes - not stuck on a stale number | Vitest | Covered (`modals/ServiceFilter/index.test.tsx`)                                                                                                           |

**Priority: Chromatic story coverage, not just Vitest.** The count badge's actual look (white
text, black circle, correct size) is a pure CSS/library-rendering concern - Vitest/jsdom can
confirm the right number and class name are present, but can't catch a library update (Mantine,
etc.) silently changing how `Text`/the `.count` class actually renders. That's exactly what
Chromatic is for, and it already runs automatically on every push to `packages/ui` - but right
now `ServiceFilter/index.stories.tsx` has exactly one story (`ServiceFilterExample = {}`, no
props, nothing selected), so Chromatic has **no rendered "with selections" state to snapshot at
all**. Adding one story variant with a few services pre-selected (so both badge types render)
is a one-time fix that then protects against this class of regression automatically, on every
future dependency bump, with no manual re-checking required - directly the ask here. Do this
before/alongside the Vitest cases above, not after.

Scroll-area height across portrait/landscape/desktop (this modal's real three-way responsive
state, not just mobile-vs-desktop) is covered once, jointly with `MoreFilter`'s identical
logic, in section 12's case 12.4 - not duplicated here.

## 8. More Filter / Attributes (`MoreFilter`)

| #   | Given                                                                                               | When                           | Expected                                                                                                                                    | Layer  | Status                                 |
| --- | --------------------------------------------------------------------------------------------------- | ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------- | ------ | -------------------------------------- |
| 8.1 | An attribute has `filterType` include                                                               | User selects it                | Added to the "Include" section and to `searchState.attributes`                                                                              | Vitest | Covered (`modals/MoreFilter.test.tsx`) |
| 8.2 | An attribute has `filterType` exclude                                                               | User selects it                | Added to the "Exclude" section, distinct from Include                                                                                       | Vitest | Covered (`modals/MoreFilter.test.tsx`) |
| 8.3 | Filter selection happens right after initial mount, while the async `values` sync is still settling | -                              | The just-applied selection is **not** clobbered by the async sync race (explicit guard exists in code for this - confirm it actually holds) | Vitest | Covered (`modals/MoreFilter.test.tsx`) |
| 8.4 | Component receives `disabled` (intl fallback pages)                                                 | Renders                        | Visibly present but non-interactive, matching `ServiceFilter`'s pattern                                                                     | Vitest | Covered (`modals/MoreFilter.test.tsx`) |
| 8.5 | No attributes selected                                                                              | Renders                        | No count badge shows - same `.count` class, same guard pattern as `ServiceFilter`                                                           | Vitest | Covered (`modals/MoreFilter.test.tsx`) |
| 8.6 | User selects N attributes (include and/or exclude)                                                  | Badge renders                  | Count badge shows the correct total                                                                                                         | Vitest | Covered (`modals/MoreFilter.test.tsx`) |
| 8.7 | Results query is loading vs. resolved                                                               | Modal's confirm button renders | Same `t('view-x-result', { count: resultCount })` pattern as `ServiceFilter` - correct, updating, pluralized                                | Vitest | Covered (`modals/MoreFilter.test.tsx`) |

Same note as §7 on both counts: the portrait/landscape/desktop scroll-height behavior is shared
with `ServiceFilter` and covered jointly in 12.4, **and** `MoreFilter.stories.tsx` has the exact
same gap - one bare `MoreFilterExample = {}` story, nothing selected, so Chromatic has no
"with selections" state to protect this component's badge either. Fix both components' stories
together; the pattern (and the fix) is identical.

## 9. Community-Focus Sidebar (`SearchResultSidebar`)

| #    | Given                                                                                                                                              | When            | Expected                                                                                                                                                                                          | Layer      | Status                                                                                                         |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------- |
| 9.1  | No focus cookie set, no legacy localStorage value                                                                                                  | Sidebar mounts  | No switches pre-selected                                                                                                                                                                          | Vitest     | Covered (`components/sections/SearchResultSidebar.test.tsx`)                                                   |
| 9.2  | A legacy localStorage value exists, no cookie yet                                                                                                  | Sidebar mounts  | Value is migrated to the cookie, localStorage key is removed, results update without a full reload                                                                                                | Vitest     | Covered (`components/sections/SearchResultSidebar.test.tsx`)                                                   |
| 9.3  | Cookie contains a stale/invalid focus ID (e.g. old `"youth"` literal)                                                                              | Sidebar mounts  | Invalid ID is dropped rather than sent to the API and silently producing a zero-effect boost                                                                                                      | Vitest     | Covered (`components/sections/SearchResultSidebar.test.tsx`)                                                   |
| 9.4  | User toggles a switch on                                                                                                                           | -               | It's persisted to the cookie, jumps to the top of the active order, and results refetch with the new `focuses`                                                                                    | Vitest     | Covered (`components/sections/SearchResultSidebar.test.tsx`)                                                   |
| 9.5  | User drag-reorders two active switches                                                                                                             | -               | New order persists to the `ir_focus_order` cookie and survives a reload                                                                                                                           | Playwright | Not started                                                                                                    |
| 9.6  | `resultCount === 0`                                                                                                                                | Sidebar renders | Focus switches are disabled but still visible (not hidden)                                                                                                                                        | Vitest     | Covered (`components/sections/SearchResultSidebar.test.tsx`)                                                   |
| 9.7  | `isAdvanced` is false (only reachable via `intl/index.tsx` currently)                                                                              | Sidebar renders | "Coming soon" overlay covers the switch group                                                                                                                                                     | Vitest     | Covered (`components/sections/SearchResultSidebar.test.tsx`)                                                   |
| 9.8  | Sidebar rendered via the mobile drawer (`onlySort`)                                                                                                | Renders         | Only the focus switches show - no result-count text (shown instead on `SortResults`' own drawer-trigger button, so not duplicated here), no org search box, no suggest link, no anti-hate message | Vitest     | Covered (`components/sections/SearchResultSidebar.test.tsx`)                                                   |
| 9.9  | `isAdvanced` is **true** - the sidebar's actual, normal, always-used-in-production state (the main results page never passes `isAdvanced={false}`) | Sidebar renders | Switches interactive, no "coming soon" overlay - the state real users see every time                                                                                                              | Chromatic  | Fixed - `AdvancedWithActiveFocus` story added in `SearchResultSidebar.stories.tsx`                             |
| 9.10 | Active, draggable switches (`isAdvanced=true`, at least one focus selected)                                                                        | Renders         | `SortableFocusSwitch` drag-handle icon (`carbon:draggable`) shows on selected switches                                                                                                            | Chromatic  | Fixed - same `AdvancedWithActiveFocus` story, via a `WithActiveFocusCookie` decorator seeding one active focus |

## 10. Sort / Mobile Drawer (`SortResults`)

`SortResults.stories.tsx` created (previously didn't exist at all - zero Chromatic coverage of
any state, not even the default). Two stories: `Closed` (default) and `Open` (via a `play`
function - `SortResults` manages its drawer's open state internally with no external `opened`
prop, so a story can't reach the open state through `args` alone).

| #    | Given                                                                 | When                | Expected                                                                                          | Layer                  | Status                                                                                                                     |
| ---- | --------------------------------------------------------------------- | ------------------- | ------------------------------------------------------------------------------------------------- | ---------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| 10.1 | Mobile viewport, results page                                         | -                   | A "sort" button is visible above the result list                                                  | Chromatic / Playwright | Covered (`tests/search/sort-drawer.spec.ts`, `SortResults.stories.tsx`)                                                    |
| 10.2 | User taps the sort button                                             | -                   | A drawer opens containing the same focus-switch controls as the desktop sidebar (`onlySort` mode) | Playwright             | Covered (`tests/search/sort-drawer.spec.ts`)                                                                               |
| 10.3 | `resultCount === 0`                                                   | Sort button renders | Button is disabled                                                                                | Vitest                 | Covered (`components/sections/SortResults.test.tsx`) - see note below: not reachable this way on the real main search page |
| 10.4 | Drawer is closed vs. open (outline button vs. drawer content visible) | Both states         | Both render correctly - the literal first thing any story for this component needs to establish   | Chromatic              | Covered - `Closed`/`Open` stories in `SortResults.stories.tsx`                                                             |

**Note on 10.3**: `SortResults.tsx` has no internal `resultCount === 0` check of its own -
`disabled` is a plain passthrough prop the caller must set. `apps/app/src/pages/search/intl/[country].tsx`
does pass `disabled={resultCount === 0}`; the main search page
(`apps/app/src/pages/search/[...params]/index.tsx`) does not. This looked like a live bug at
first, but isn't reachable in practice: that page shows `<NoResults>` instead of the results
block (`SortResults` included) whenever `resultCount === 0`, gated on
`api.organization.getNatlCrisis({ cca2: country })` - a country-level query, confirmed via a real
zero-result US search (`tests/search/sort-drawer.spec.ts`'s investigation) to always return data
for `US`. Documented, not filed as a bug.

## 11. Pagination

| #    | Given                                                           | When           | Expected                                                                                                         | Layer      | Status                                                                                                                                                |
| ---- | --------------------------------------------------------------- | -------------- | ---------------------------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| 11.1 | More results exist than fit on one page                         | Results render | Pagination control shows the correct total page count                                                            | Vitest     | Covered (`components/core/Pagination.test.tsx`)                                                                                                       |
| 11.2 | User is on a page below the last page                           | -              | The next page's data is prefetched in the background (verify via network assertion, not just "it doesn't error") | Playwright | Covered (`tests/search/pagination.spec.ts`) - **failing, confirms a real bug, filed as [#2065](https://github.com/weareinreach/InReach/issues/2065)** |
| 11.3 | User clicks a page number                                       | -              | `?page=` updates via shallow route change; page scrolls to top; no full `getServerSideProps` re-run              | Playwright | Covered - URL-update already covered by `tests/search/url-routing.spec.ts` case 1.5; scroll-to-top covered by `tests/search/pagination.spec.ts`       |
| 11.4 | Fewer results than one page's worth                             | Results render | Pagination control doesn't show (or shows as inert) - decide and document the expected presentation              | Vitest     | Covered (`components/core/Pagination.test.tsx`) - documented actual behavior: it still renders (page "1", both Prev/Next inert), doesn't hide         |
| 11.5 | Router `page` query set to the last page (e.g. page 8 of 8)     | Renders        | "Next" shows its disabled/grey styling                                                                           | Chromatic  | Fixed - `LastPage` story added in `Pagination.stories.tsx`                                                                                            |
| 11.6 | Active page is a middle page, not a boundary (e.g. page 4 of 8) | Renders        | Both left and right ellipsis dots show simultaneously                                                            | Chromatic  | Fixed - `MiddlePage` story added in `Pagination.stories.tsx`                                                                                          |

## 12. Responsive / Breakpoint Behavior

See the main testing doc's [Plan section](README.md#test-automation-plan) for the
three-layer split (JS-conditional vs. CSS-breakpoint vs. real-flow). Cases specific to
search:

**Important caveat found while auditing this section**: on the main results page
(`[...params]/index.tsx`), `isMobile` is checked only against the `xs` breakpoint (500px) -
there is no separate JS-level "tablet" state there. Everything from 500px up to desktop
widths falls into the single `isMobile === false` branch. So for that page, "tablet" isn't a
distinct _logic_ state to test (12.1/12.2 below already cover its only two JS branches) - the
tablet-specific risk is entirely in the **CSS layer** (does the layout actually look right at
tablet width, even though the JS branch is the same as desktop) and in the one page that
_does_ have a real tablet-only branch (13.x below).

| #    | Given                                                                                                                                     | When                                                                                                                                                     | Expected                                                                                                                                                                                                                                                | Layer                                                  | Status                                                                                                                                                                                                                                                                                |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 12.1 | Viewport below the `xs` breakpoint (500px)                                                                                                | Results page renders                                                                                                                                     | `isMobile` branch renders: sidebar visually hidden (`hideMobile`), mobile result-count row and sort-drawer trigger appear                                                                                                                               | Playwright (see note)                                  | Covered (`tests/search/responsive.spec.ts`)                                                                                                                                                                                                                                           |
| 12.2 | Viewport at/above `xs` (this is the only branch tablet and desktop widths both fall into on this page)                                    | Results page renders                                                                                                                                     | Desktop branch: sidebar shown as a persistent column, no sort-drawer trigger - confirm this looks/works correctly at tablet widths too, not just desktop, since the JS treats them identically                                                          | Playwright (see note)                                  | Covered (`tests/search/responsive.spec.ts`)                                                                                                                                                                                                                                           |
| 12.3 | `SearchBox`, `SearchResultCard`, `SearchResultSidebar`, `SearchDistance` Storybook stories, snapshotted at the theme's actual breakpoints | Snapshotted at **`xs` (500, phone)**, **`sm`/`md` (768/1024, tablet - use the `ipad`/`ipad10p` entries already defined in `viewports.ts`)**, and desktop | Layout is correct at each, not just the Storybook default viewport - specifically check `SearchResultSidebar`'s and the results page's `Grid.Col span={{ base: 12, sm: 8, md: 8 }}` column-width shift, which is pure CSS and invisible to Vitest/jsdom | Chromatic                                              | Fixed - `chromatic.viewports: [500, 768, 1024, 1440]` added to all four stories' meta parameters                                                                                                                                                                                      |
| 12.4 | `ServiceFilter`/`MoreFilter` modals                                                                                                       | Opened in portrait, in landscape (`(orientation: landscape) and (max-height: 430px)` - the exact query the component itself uses), and at desktop width  | Scroll-area max-height adapts correctly in all three states, not just "mobile vs desktop" - landscape is the state most likely to actually occur on a tablet                                                                                            | Vitest (mock `matchMedia` for each query) or Chromatic | Covered (`modals/ServiceFilter/index.test.tsx` case 12.4) - confirms the landscape branch renders without crashing; exact computed pixel height not asserted (fragile, low value - Mantine's ScrollArea.Autosize doesn't expose `mah` as a simple, stable DOM attribute to read back) |
| 12.5 | Real mobile browser viewport                                                                                                              | Full search flow attempted (search → filter → view result → save)                                                                                        | Every step is completable - no overlapping elements, no unreachable tap targets                                                                                                                                                                         | Playwright (mobile project)                            | Covered (`tests/search/device-flow.spec.ts`, `mobile` project added to `playwright.config.ts`)                                                                                                                                                                                        |
| 12.6 | Real tablet browser viewport (e.g. `devices['iPad (gen 7)']`)                                                                             | Same full search flow                                                                                                                                    | Same bar as 12.5 - tablet is a distinct real-device class from both phone and desktop, worth its own Playwright project rather than assuming phone or desktop coverage stands in for it                                                                 | Playwright (tablet project)                            | Covered (`tests/search/device-flow.spec.ts`, `tablet` project added to `playwright.config.ts`)                                                                                                                                                                                        |

**Note on 12.1/12.2's layer**: written up as Vitest ("mock `matchMedia`") originally, but that
would mean building a new harness for rendering `apps/app`'s page component directly - no such
harness exists yet (unlike `packages/ui`'s component-level Vitest setup). Playwright against the
real page, resizing the actual viewport, exercises the exact same `useMediaQuery` code path with
no new infrastructure, and is already the established pattern here.

**Real finding from 12.1/12.2's investigation**: between 500px and 768px, NEITHER branch's UI
renders - the mobile-only elements are gated on the 500px JS breakpoint (already `false` here) and
the sidebar is gated on a _different_, 768px CSS breakpoint (not yet crossed either). Confirmed
live and filed as [#2066](https://github.com/weareinreach/InReach/issues/2066), with its own test
(`tests/search/responsive.spec.ts`'s `12.dead-zone` case).

## 13. International / Out-of-Service-Area Fallback

`search/intl/index.tsx` (no country) and `search/intl/[country].tsx` (specific country).

| #    | Given                                                          | When                                                                                                                                           | Expected                                                                                                                                                                                                                                               | Layer                               | Status                                                                                                                                       |
| ---- | -------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| 13.1 | Visitor's location resolves to a country InReach doesn't serve | Landed on `intl/[country]`                                                                                                                     | Page shows the localized country name via `Intl.DisplayNames`, with international crisis resources                                                                                                                                                     | Playwright                          | Not started                                                                                                                                  |
| 13.2 | `[country]` is US, CA, or MX                                   | Page mounts                                                                                                                                    | Client-side redirect to the real `/search/[...params]` results page with a zeroed default tuple                                                                                                                                                        | Playwright                          | Not started                                                                                                                                  |
| 13.3 | Any intl fallback page                                         | Filters render                                                                                                                                 | `ServiceFilter`/`MoreFilter` are visible but disabled/inert - not clickable, not misleadingly interactive                                                                                                                                              | Vitest                              | Not started                                                                                                                                  |
| 13.4 | `intl/index.tsx` specifically (no country context)             | Sidebar renders                                                                                                                                | Focus-switch group shows the "coming soon" overlay (`isAdvanced` defaults false here)                                                                                                                                                                  | Vitest                              | Not started                                                                                                                                  |
| 13.5 | `intl/[country].tsx` specifically                              | Sidebar renders                                                                                                                                | Focus-switch group is **not** overlaid (this page passes `isAdvanged={true}`) but is still functionally inert since `resultCount` is always 0 - decide if this inconsistency between the two intl pages is intentional before locking in as "expected" | Vitest                              | Not started - flag for product/design confirmation                                                                                           |
| 13.6 | `[country]` param fails validation (not a 2-char string)       | `getServerSideProps` runs                                                                                                                      | Returns a real 404, not a broken render                                                                                                                                                                                                                | Playwright                          | Not started                                                                                                                                  |
| 13.7 | `intl/index.tsx` specifically                                  | Viewport is at/below the `sm` breakpoint (768px) - this page's own `isTablet` check, distinct from the `xs` check every other search page uses | A result-count row + divider renders below the search controls                                                                                                                                                                                         | Vitest (mock `matchMedia` for `sm`) | Not started - this is the one genuine tablet-only JS branch found anywhere in the search surface, and had no test case until this audit pass |
| 13.8 | `intl/index.tsx` specifically                                  | Viewport is above `sm`                                                                                                                         | The result-count row does not render                                                                                                                                                                                                                   | Vitest                              | Not started                                                                                                                                  |
| 13.9 | `intl/index.tsx`                                               | Viewport is exactly at the `sm` boundary (768px)                                                                                               | Confirm which side of the boundary `max-width: sm` actually lands on (768px itself: row shows or doesn't) - worth a dedicated boundary test since off-by-one breakpoint bugs are easy to introduce silently in a future edit                           | Vitest                              | Not started                                                                                                                                  |

## 14. Location-Based Alert Banner (`LocationBasedAlertBanner`)

Fetches `LocationAlert` records (admin/CMS-authored content, not code) matching the
searched coordinates via a PostGIS lookup; content and any links are DB-driven, not
hardcoded. Rendered twice - `type='primary'` and `type='secondary'` - each showing only
alerts whose `level` enum suffix matches its type, so a `WARN_PRIMARY` alert never appears
in the secondary slot or vice versa.

**Serious story bug found auditing this section, not just a coverage gap**: none of the 3
existing stories (`Statewide`, `CountyLevel`, `Nationwide`) ever sets the required `type` prop.
`RenderWrapper`'s `type` is therefore `undefined`, and the component's own filter
(`alertProps.level.toLowerCase().endsWith(type)`) coerces that to the string `"undefined"`,
which never matches any mock alert's level suffix (`_PRIMARY`). **All three existing stories
render zero alert boxes** - no color state, no container-margin class, nothing - despite each
story's name implying it demonstrates a specific alert. This is worse than "not started": it's
a story that looks like coverage but isn't, which is easy to mistake for done. Fixing the
missing `type` prop is a prerequisite for every Chromatic case below, and separately, the
`Nationwide` story's `<Link>`-containing alert can't be fixed to render even after that, because
`component.LocationBasedAlertBanner.json` (the mock data) contains **only** `*_PRIMARY`-level
alerts - no `*_SECONDARY` entries exist at all, so 14.9 below needs new mock data too, not just
a prop fix.

| #    | Given                                                                                               | When                                                              | Expected                                                                                                                                                                                                                                                                                                                                                                  | Layer     | Status                                                                                                                                                                     |
| ---- | --------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 14.1 | `lat`/`lon` resolve to one of the listed US territories/states (PW, AS, UM, MP, MH, US, VI, GU, PR) | Results page renders                                              | Extra top margin/spacing applied around the banner                                                                                                                                                                                                                                                                                                                        | Chromatic | Not started                                                                                                                                                                |
| 14.2 | `lat`/`lon` resolve elsewhere                                                                       | Results page renders                                              | Banner still renders (both `primary` and `secondary` types are unconditional) but without the extra spacing                                                                                                                                                                                                                                                               | Chromatic | Not started                                                                                                                                                                |
| 14.3 | An active alert's text includes a `<Link>` tag                                                      | Banner renders                                                    | The link is always treated as external and opens in a new tab (`target='_blank'`), even if its destination happens to be an internal-looking path - this is unconditional in the component, not content-dependent                                                                                                                                                         | Vitest    | Not started                                                                                                                                                                |
| 14.4 | An active alert's text has no `<Link>` tag                                                          | Banner renders                                                    | Plain text only, nothing clickable in that alert                                                                                                                                                                                                                                                                                                                          | Vitest    | Not started                                                                                                                                                                |
| 14.5 | Alerts of both primary and secondary level match the same location                                  | Results page renders                                              | Primary-level alerts show only in the `type='primary'` instance (above the search controls); secondary-level only in the `type='secondary'` instance (within the results column) - no bleed between the two                                                                                                                                                               | Vitest    | Not started                                                                                                                                                                |
| 14.6 | No active `LocationAlert` matches the searched coordinates                                          | Results page renders                                              | Neither banner instance shows any content - confirm this renders as genuinely empty (no empty box/border) rather than an awkward blank space                                                                                                                                                                                                                              | Vitest    | Not started                                                                                                                                                                |
| 14.7 | An alert is showing                                                                                 | User revisits the same location later (same session or a new one) | The alert reappears identically every time - there is no dismiss/close action or persistence of any kind in this component, unlike the anti-hate modal or cookie consent banner (see [site-chrome-test-inventory.md](site-chrome-test-inventory.md)). Confirm this always-reappears behavior is intended for a location-safety alert (plausible) rather than an oversight | Manual    | Not started - confirm intent before treating as fixed expected behavior                                                                                                    |
| 14.8 | `Statewide`/`CountyLevel`/`Nationwide` stories, with the `type` prop bug fixed                      | Snapshotted                                                       | Each story's named alert level (`INFO_PRIMARY`/`WARN_PRIMARY`/`CRITICAL_PRIMARY`) renders with its correct color state (cornflower/pink bg etc. per level)                                                                                                                                                                                                                | Chromatic | Not started - blocked on the `type` prop fix above; currently these stories are indistinguishable empty boxes in Chromatic despite their names                             |
| 14.9 | A `*_SECONDARY`-level alert (`type='secondary'`, e.g. the results-column banner)                    | Snapshotted                                                       | Secondary-type container/margin classes and color states render correctly, same as primary                                                                                                                                                                                                                                                                                | Chromatic | Not started - blocked on both the `type` prop fix AND adding at least one `*_SECONDARY` mock alert to `component.LocationBasedAlertBanner.json`, which doesn't exist today |

## 15. Known Non-Goals / Dead Code (do not write tests here until re-enabled)

Flagged explicitly so these aren't silently assumed covered, and so nobody spends time
testing something not actually reachable by users:

- **`SearchDistance.tsx`** (radius slider + "include remote" checkbox) - not rendered
  anywhere reachable from the live search page (commented out in `SearchResultSidebar`).
  Its own param schema also doesn't match the current route's actual segment order, so it
  would likely need a real fix, not just an uncomment, before it could be re-enabled.
- **`isAdvanced = false` path on the main results page** - the main results page hardcodes
  `isAdvanced = true` with no code path that sets it otherwise; the `false` branch (tier
  dividers suppressed, etc.) is only actually reachable via `search/intl/index.tsx`, not the
  primary flow. Covered under section 13, not section 4.
- **Map view** - no map component is wired into the search results page at all (`GoogleMap`
  exists in the UI package but isn't imported here). Not a gap to test; there's no feature
  here yet.
- **URL-synced filters** - `services`/`attributes`/community-focus are _not_ reflected in the
  URL on the results page (only location/radius/unit/country and page number are). A
  `getRoute()` helper that _would_ serialize them exists but is only used elsewhere
  (`MobileNav`, `Breadcrumb`) for "back to search" links. If "shareable filtered search URL"
  is a real expectation, that's a feature gap, not a test gap - resolve as a product decision
  before adding a test case that would just fail against current-by-design behavior.

---

_Companion to [docs/Testing/README.md](README.md). Built from a read of the actual
`apps/app/src/pages/search/**` and related `packages/ui` components as of 2026-09-20 - update
both the inventory and that date if the underlying components change shape._
