{
	"$schema": "https://turbo.build/schema.json",
	"extends": ["//"],
	"pipeline": {
		"generate:data": {
			"cache": false,
			"dependsOn": ["^db:generate", "^db:deploy"],
			"env": ["DATABASE_URL"],
			"outputs": ["./generated/**"]
		}
	}
}
